rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Demo app has no Firebase Auth sign-in (uses a fixed demo user).
    // Open access for development. Lock this down before any real launch.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
